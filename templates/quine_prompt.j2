{# Quine prompt template - self-reproducing prompt #}
# Genesis Quine - Self-Reproducing Repository

This prompt contains the complete source code for the Genesis project.
When executed by Claude Code, it will recreate the entire repository structure.

## Repository Manifest

- **Project:** {{ root_name }}
- **Files:** {{ file_count }}
- **Checksum:** `{{ total_checksum }}`

## Instructions for Claude Code

Execute the following steps to recreate this repository:

1. Create the directory structure
2. Write each file with its exact content
3. Verify the checksum matches

## File Manifest

{% for file in files %}
### `{{ file.path }}`
**Checksum:** `{{ file.checksum }}`

```{% if file.path.endswith('.py') %}python{% elif file.path.endswith('.toml') %}toml{% elif file.path.endswith('.j2') %}jinja2{% elif file.path.endswith('.md') %}markdown{% elif file.path.endswith('.yaml') or file.path.endswith('.yml') %}yaml{% endif %}

{{ file.content }}
```

{% endfor %}

## Verification

After creating all files, the combined checksum should be: `{{ total_checksum }}`

To verify:
1. Read all files in sorted order by path
2. Compute SHA256 of each file's content (first 16 chars)
3. Concatenate all checksums
4. Compute SHA256 of concatenated string (first 32 chars)
5. Compare with expected checksum

## Recreation Command

To recreate this repository structure, use the following approach:

```python
# Create directories
directories = [{% for file in files %}{% set parts = file.path.split('/') %}{% if parts | length > 1 %}"{{ parts[:-1] | join('/') }}", {% endif %}{% endfor %}]
for d in set(directories):
    Path(d).mkdir(parents=True, exist_ok=True)

# Write files (content provided above)
# ...
```

---
*Generated by Genesis v0.1.0*
*This is a quine: prompt → repo → prompt*
